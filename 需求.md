下面这份可以直接复制给 Cursor / Claude Code / ChatGPT / v0 开始做项目。

---

# AI Price Watch 项目启动需求文档

## 一、项目基础信息

项目名称：**AI Price Watch**

域名：`price.lengziyu.cn`

GitHub 仓库：
`https://github.com/lengziyu/ai-price-watch.git`

项目定位：

> 一个面向 AI 用户、开发者和独立开发者的大模型价格情报站，帮助用户快速比较大模型订阅价格、API Token 价格、会员差价、AI 薅羊毛活动，并轻量推荐 AI 使用场景。

核心关键词：

* AI 大模型比价
* ChatGPT / Claude / Gemini 等会员订阅比价
* API Token 价格对比
* AI 羊毛 / 免费额度 / 优惠活动
* AI 工具轻导航
* 开发者风
* 极简
* Vercel / Linear 风格

---

## 二、目标用户

主要用户：

1. AI 开发者
2. 独立开发者
3. 经常使用 ChatGPT、Claude、Gemini、Cursor 等 AI 工具的人
4. 想低成本使用 AI 的普通用户
5. 关注 AI 订阅、API、优惠活动的人

用户核心需求：

* 哪个 AI 模型更便宜？
* ChatGPT / Claude / Gemini 会员哪里买更划算？
* 哪个平台有活动？
* 哪些 AI 工具有免费额度？
* API Token 价格怎么比？
* 哪个模型适合写代码、办公、写作、学习？

---

## 三、第一版 MVP 功能

第一版只做核心功能，不做太复杂。

### 1. 首页

首页包含：

* Hero 区域
* 大模型会员订阅比价入口
* API Token 比价入口
* 今日 AI 羊毛入口
* AI 使用场景推荐入口
* 最新更新列表

首页文案方向：

```txt
AI Price Watch
追踪大模型价格、会员订阅差价、API Token 成本与 AI 优惠活动。
帮你更便宜、更高效地使用 AI。
```

副标题：

```txt
Compare AI subscriptions, token prices, free credits and AI deals.
```

---

### 2. 大模型会员订阅比价

展示 ChatGPT、Claude、Gemini、Grok、Poe、Cursor 等产品的订阅价格。

字段建议：

```ts
type SubscriptionPlan = {
  id: string
  productName: string
  provider: string
  planName: string
  officialPriceUSD?: number
  priceCNY?: number
  billingCycle: 'monthly' | 'yearly'
  region?: string
  sourceUrl?: string
  note?: string
  tags: string[]
  updatedAt: string
}
```

展示字段：

* 产品名称
* 订阅方案
* 官方价格
* 折合人民币
* 地区差价
* 是否有活动
* 备注
* 更新时间

示例产品：

* ChatGPT Plus
* ChatGPT Pro
* Claude Pro
* Claude Max
* Gemini Advanced
* Grok
* Poe
* Cursor Pro
* Windsurf

---

### 3. API Token 比价

展示不同模型的输入/输出 Token 价格。

字段建议：

```ts
type TokenPrice = {
  id: string
  modelName: string
  provider: string
  platform: string
  inputPricePer1M: number
  outputPricePer1M: number
  currency: 'USD' | 'CNY'
  contextWindow?: string
  category: 'text' | 'vision' | 'audio' | 'embedding' | 'reasoning'
  sourceUrl?: string
  note?: string
  updatedAt: string
}
```

页面功能：

* 按供应商筛选
* 按平台筛选
* 按价格排序
* 按模型类型筛选
* 显示更新时间
* 显示来源链接

首批模型可以手动维护：

* OpenAI GPT 系列
* Claude 系列
* Gemini 系列
* DeepSeek 系列
* Qwen 系列
* Kimi
* Doubao
* Grok

---

### 4. AI 羊毛 / 优惠活动

用于收录 AI 免费额度、折扣、限时活动、教育优惠、新用户权益。

字段建议：

```ts
type AIDeal = {
  id: string
  title: string
  provider: string
  summary: string
  dealType: 'free_credit' | 'discount' | 'trial' | 'student' | 'region_price' | 'other'
  value?: string
  deadline?: string
  sourceUrl?: string
  howToGet?: string
  suitableFor: string[]
  riskLevel: 'low' | 'medium' | 'high'
  status: 'active' | 'expired' | 'unknown'
  updatedAt: string
}
```

展示内容：

* 活动标题
* 活动摘要
* 领取方式
* 适合人群
* 截止时间
* 风险提示
* 来源链接

注意：

不要鼓励灰产、盗号、违规代充。
可以收录正规活动、官方优惠、地区价格差异、教育优惠、新用户额度。

---

### 5. AI 使用场景推荐

这个模块轻量展示，不要喧宾夺主。

场景包括：

* 写代码
* 写作
* 做 PPT
* 翻译
* 学习
* 数据分析
* 办公自动化
* 图片生成
* 视频生成

字段建议：

```ts
type UseCase = {
  id: string
  title: string
  description: string
  recommendedTools: string[]
  recommendedModels: string[]
  difficulty: 'easy' | 'medium' | 'advanced'
  estimatedCost: 'free' | 'low' | 'medium' | 'high'
}
```

---

## 四、页面结构

建议路由：

```txt
/
首页

/pricing/subscriptions
会员订阅比价

/pricing/tokens
API Token 比价

/deals
AI 羊毛 / 优惠活动

/tools
AI 工具轻导航

/use-cases
AI 使用场景推荐

/about
关于本站
```

---

## 五、设计风格

整体风格：

* 极简
* 开发者风
* 类似 Vercel / Linear
* 黑白灰为主
* 少量蓝色/紫色点缀
* 大量留白
* 卡片式布局
* 数据表格清晰
* PC 和 H5 都要兼容

设计关键词：

```txt
minimal
clean
developer-focused
data-driven
Vercel-like
Linear-like
responsive
```

移动端要求：

* 表格在 H5 下改成卡片展示
* 筛选项可折叠
* 重点信息优先展示
* 按钮足够大
* 页面加载快

---

## 六、技术栈建议

请使用：

```txt
Next.js App Router
TypeScript
Tailwind CSS
shadcn/ui
Lucide Icons
Supabase
Vercel
```

如果第一版不接数据库，可以先用本地 JSON / TypeScript mock 数据。

推荐目录结构：

```txt
src/
  app/
    page.tsx
    pricing/
      subscriptions/
        page.tsx
      tokens/
        page.tsx
    deals/
      page.tsx
    tools/
      page.tsx
    use-cases/
      page.tsx
    about/
      page.tsx

  components/
    layout/
    pricing/
    deals/
    tools/
    ui/

  data/
    subscriptions.ts
    token-prices.ts
    deals.ts
    tools.ts
    use-cases.ts

  lib/
    utils.ts
    format.ts

  types/
    index.ts
```

---

## 七、第一版优先级

优先做：

1. 首页
2. API Token 比价页
3. 会员订阅比价页
4. AI 羊毛页
5. 移动端适配
6. SEO 基础优化

暂时不做：

* 登录
* 用户投稿
* 后台管理
* 自动爬虫
* 支付
* 复杂推荐算法

---

## 八、SEO 要求

每个页面需要基础 SEO。

首页标题：

```txt
AI Price Watch - 大模型价格、Token 成本与 AI 优惠活动追踪
```

描述：

```txt
AI Price Watch 提供 ChatGPT、Claude、Gemini、DeepSeek 等大模型订阅价格、API Token 价格、免费额度和 AI 优惠活动对比，帮助你低成本使用 AI。
```

关键词：

```txt
AI比价, 大模型价格, ChatGPT价格, Claude价格, Gemini价格, Token价格, AI羊毛, AI优惠, AI工具推荐
```

---

## 九、数据来源原则

所有价格和活动需要保留来源链接。

每条信息都要有：

* 来源
* 更新时间
* 备注
* 是否过期

页面中需要显示：

```txt
价格可能随时间变化，请以官方页面为准。
```

---

## 十、请 AI 先完成的任务

请先基于以上需求，完成：

1. 初始化 Next.js + TypeScript + Tailwind 项目结构
2. 设计首页
3. 设计 Token 价格表页面
4. 设计订阅比价页面
5. 设计 AI 羊毛列表页面
6. 使用 mock 数据填充
7. 保证 PC 和移动端体验良好
8. 保持 Vercel / Linear 风格
9. 代码清晰、组件拆分合理
10. 后续方便接 Supabase 数据库

第一版目标：

> 先做出一个可以上线的静态 MVP，后面再逐步接入数据库、AI 总结、自动采集和后台管理。
